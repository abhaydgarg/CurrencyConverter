/**
 * Style to be used in multiple elements.
 */

import { StyleSheet } from 'react-native';

export default function (theme) {
  return StyleSheet.create({
    container: {
      flex: 1
    },
    textCenter: {
      textAlign: 'center'
    }
  });
}
